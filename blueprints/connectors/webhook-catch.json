{
  "$schema": "https://loxtep.com/schemas/blueprint/connector.json",
  "blueprint_type": "connector",
  "slug": "webhook-catch",
  "name": "Webhook Catch Connector",
  "description": "Generic webhook endpoint for receiving HTTP POST events from any external system. Supports API key, HMAC, bearer token, and basic authentication.",
  "version": "1.0.0",
  "tags": ["webhook", "http", "events", "generic"],
  "use_cases": [
    "Receive events from any webhook source",
    "Custom integration endpoints",
    "Third-party notification handling",
    "Event-driven architecture ingestion"
  ],
  "entity": {
    "connector_id": "{{CONNECTOR_ID}}",
    "organization_id": "{{ORGANIZATION_ID}}",
    "connector_type": "webhook",
    "name": "Webhook Catch",
    "description": "Generic webhook connector for receiving HTTP POST events with configurable authentication",
    "category": "custom",
    "icon_url": null,
    "documentation_url": "https://docs.loxtep.com/connectors/webhook",
    "auth_type": "api_key",
    "oauth_config": null,
    "supported_directions": ["inbound"],
    "supported_entities": ["events"],
    "supports_webhooks": true,
    "supports_polling": false,
    "required_scopes": null,
    "required_permissions": null,
    "version": "1.0.0",
    "is_beta": false,
    "metadata": {
      "blueprint_slug": "webhook-catch",
      "supported_auth_types": ["api_key", "hmac", "bearer", "basic", "none"]
    },
    "created_at": "{{CREATED_AT}}",
    "updated_at": "{{UPDATED_AT}}"
  },
  "connection_template": {
    "type": "webhook",
    "configuration": {
      "endpoint_slug": "{{ENDPOINT_SLUG}}",
      "auth_type": "{{AUTH_TYPE}}",
      "include_event_id": true,
      "timeout_seconds": 30
    },
    "credential_fields": {
      "api_key": "{{API_KEY}}"
    }
  },
  "placeholders": {
    "{{CONNECTOR_ID}}": "Generated UUID for the connector",
    "{{ORGANIZATION_ID}}": "Organization that owns this connector",
    "{{ENDPOINT_SLUG}}": "URL slug for the webhook endpoint",
    "{{AUTH_TYPE}}": "Authentication type: api_key, hmac, bearer, basic, or none",
    "{{API_KEY}}": "API key for authentication (encrypted)",
    "{{CREATED_AT}}": "ISO 8601 timestamp",
    "{{UPDATED_AT}}": "ISO 8601 timestamp"
  }
}
